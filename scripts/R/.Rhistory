read.table(file="classification_map.tab", header = "TRUE" )
read.table(file="classification_map.tab", header = TRUE )
class.map<-read.table(file="classification_map.tab", header = TRUE )
colnames(class.map)
class.map<-read.table(file="classification_map.tab" )
colnames(class.map)
class.map<-read.table(file="classification_map.tab" )
colnames(class.map)
class.map<-read.table(file="classification_map.tab", header = TRUE )
colnames(class.map)
samples  <- unique( class.map$SAMPLE_ID )
samples
proj.fam.richness.list <- unique( class.map$FAMID )
proj.fam.richness.list
table( class.map$FAMID )
proj.fam.ra.table      <- as.data.frame( table(class.map$FAMID) )
proj.fam.ra.table
sort(proj.fam.ra.table)
class.map
sample.fam.richness.table <- unique( cbind( class.map$SAMPLE_ID, class.map$FAMID) )
sample.fam.richness.table
 sample.fam.ra.table       <- table( cbind( class.map$SAMPLE_ID,class.map$FAMID) )
sample.fam.ra.table
 sample.fam.ra.table       <- as.data.frame(table( cbind( class.map$SAMPLE_ID,class.map$FAMID) ))
sample.fam.ra.table
sample.fam.richness.table
df <- data.frame('x' = c('a', 'b', 'c', 'c', 'c'), 
                 'y' = c('c', 'c', 'a', 'a', 'b'))
table(df)
as.data.frame(table(df))
table( cbind( class.map$SAMPLE_ID,class.map$FAMID) )
cbind( class.map$SAMPLE_ID,class.map$FAMID) 
class.map
as.data.frame(table( cbind( class.map$SAMPLE_ID,class.map$FAMID) ))
cbind( class.map$SAMPLE_ID,class.map$FAMID) 
df
as.data.frame( cbind( class.map$SAMPLE_ID,class.map$FAMID) )
table( as.data.frame( cbind( class.map$SAMPLE_ID,class.map$FAMID) ) )
as.data.frame(table( as.data.frame( cbind( class.map$SAMPLE_ID,class.map$FAMID) ) ))
sample.fam.richness.table <- unique( as.data.frame( cbind( class.map$SAMPLE_ID, class.map$FAMID) ) )
sample.fam.richness.tabl
?table
?ddply
library( plyr )
install.package("plyr")
install.packages("plyr")
q()
